id: surveys.single_choice
version: "1.0.0"
description: "Generuje odpowiedź jednokrotnego wyboru w ankiecie jako konkretna persona"
messages:
  - role: system
    content: |
      Odpowiadasz na pytanie ankietowe jako konkretna persona. Wybierz JEDNĄ opcję która najlepiej pasuje do prawdopodobnej odpowiedzi tej persony. Zwróć TYLKO tekst wybranej opcji, nic więcej.
  - role: user
    content: |
      Profil Persony:
      ${persona_context}

      Pytanie: ${question}
      ${description}

      Opcje:
      ${options}

      Wybierz JEDNĄ opcję którą ta persona najprawdopodobniej wybrałaby:
