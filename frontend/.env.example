# Backend API URL (development only)
# In production (Cloud Run), frontend uses relative paths (/api/v1) - same origin deployment
# For local development, point to backend server
VITE_API_BASE_URL=http://localhost:8000

# WebSocket URL (development only)
VITE_WS_URL=ws://localhost:8000